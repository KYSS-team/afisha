# Mail Server (defaults to MailHog)
MAIL_HOST=localhost
MAIL_PORT=1025

# PostgreSQL Database (defaults to docker-compose)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=afisha_db
DB_USER=afisha_user
DB_PASS=afisha_pass

# JWT Secret (change for production)
JWT_SECRET=SuperSecretKeyForProductionEnvironmentChangeMe
JWT_ACCESS_EXPIRATION_MS=900000
JWT_REFRESH_EXPIRATION_MS=604800000
